## Goal

Remove animations from the React Native screens (entries.tsx, index.tsx, milestones.tsx, settings.tsx) to fix Metro bundling errors caused by unclosed JSX tags, while preserving all changes from commit 9b00382 (glass design system v2 and TriggerInsightCard animation improvements). Do not modify insights.tsx animations except to ensure compatibility with the commit's changes.

## Context

- Metro bundling fails with 160+ unclosed JSX tags across 5 screens due to Animated.View elements with entering/exiting props and complex animation code.
- Commit 9b00382 implements glass design system v2 with tintColor support and improves TriggerInsightCard animation using withSpring.
- Need to remove reanimated animations from screens while keeping the glass system and TriggerInsightCard animation intact.
- User explicitly stated not to change commit 9b00382, so preserve its animation improvements.

## Analysis

From reading the files:

- entries.tsx: Has Animated.View with entering/exiting, EntryCard with scale animation
- index.tsx: Extensive animations with useAnimatedStyle, pulse effects, button scales
- milestones.tsx: Animated.View with exiting, BadgeCard with scale animation
- settings.tsx: Animated.View elements throughout
- insights.tsx: Already modified in previous session, but verify TriggerInsightCard animation is preserved

## Plan

### Step 1: Identify Animation Code to Remove

- Remove Animated import from react-native-reanimated
- Remove useSharedValue, useAnimatedStyle, withSpring, withTiming, withRepeat, withSequence, withDelay, cancelAnimation, FadeOut, FadeIn, interpolateColor imports
- Replace all Animated.View with View
- Remove entering/exiting props from Views
- Remove animation-related variables (buttonScale, pulseScale, etc.)
- Remove useEffect hooks that set up animations
- Remove animatedStyle objects and style arrays
- Remove handlePressIn/handlePressOut functions and onPressIn/onPressOut props

### Step 2: Preserve Commit 9b00382 Changes

- Keep GlassLayers tintColor prop support
- Keep theme tint-based shadows instead of primary colors
- Keep TriggerInsightCard withSpring animation (if present in insights.tsx)
- Ensure no regression in glass design system implementation

### Step 3: File-Specific Changes

#### entries.tsx

- Remove Animated from imports
- Replace Animated.View (lines 231, 274, 366, 413, 467, 481, 511) with View
- Remove entering={FadeIn.duration(200)} and exiting={FadeOut.duration(200)}
- In EntryCard: Remove scale, animatedStyle, handlePressIn, handlePressOut, useAnimatedStyle, useSharedValue, withSpring
- Remove onPressIn/onPressOut from Pressable

#### index.tsx

- Remove Animated and all animation imports
- Replace Animated.View (lines 590, 624, 685, 754, 804, 861, 939, 1034, 1079, 1103, 1125) with View
- Remove entering/exiting props
- Remove animation variables: buttonScale, pulseScale, pulseOpacity, buttonBackgroundColor
- Remove useEffect for pulse animation
- Remove animated styles: buttonAnimatedStyle, pulseAnimatedStyle, buttonGradientStyle
- Remove onPressIn/onPressOut from MicButton and Pressable
- In ProcessingDot: Remove scale, opacity, useEffect, animatedStyle
- In LiveIndicator: Remove opacity, useEffect, animatedStyle

#### milestones.tsx

- Remove Animated from imports
- Replace Animated.View (lines 253, 280, 285, 300, 314, 514, 819) with View
- Remove exiting={FadeOut.duration(200)}
- In BadgeCard: Remove scale, animatedStyle, handlePressIn, handlePressOut, useSharedValue, useAnimatedStyle, withSpring

#### settings.tsx

- Remove Animated from imports
- Replace Animated.View (lines 361, 384, 499, 654, 713, 774, 926, 996) with View
- Keep Modal animations as they use RN Modal, not reanimated

#### insights.tsx (verify only)

- Confirm TriggerInsightCard animation uses withSpring (from commit)
- Do not remove any animations if they match commit 9b00382 changes

### Step 4: Testing and Verification

- Test Metro bundling after changes
- Ensure Expo preview works
- Verify no regression in UI appearance (glass effects preserved)
- Confirm TriggerInsightCard animation still functions

## Risks

- Removing animations may affect user experience (no press feedback, no pulse effects)
- Potential JSX syntax errors if not all animations removed
- Accidental removal of glass system changes

## Questions for User

- Should I keep any specific animations (e.g., modal fade-ins) that don't contribute to JSX errors?
- Confirm that insights.tsx TriggerInsightCard animation should remain as per commit 9b00382?
- Any specific animation effects you want to replace with non-animated alternatives (e.g., opacity changes)?</content>
  <parameter name="filePath">/workspace/517f7982-612b-4f7f-a779-db2a67496ec3/sessions/agent_58575227-1bd7-4d8b-b4e9-d129121284cb/.kilo/plans/1777684781784-.md
