{
  "name": "vocolens-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd /app && EXPO_TUNNEL=none EXPO_OFFLINE=true EXPO_NO_DEV_CLIENT=true npx expo start --web --port ${PORT:-3000} --offline"
  }
}
